# ---------------------------------------------------------------------------
# LOCAL SECRETS TEMPLATE
# ---------------------------------------------------------------------------
# 1. Copy this file to:   .streamlit/secrets.toml
# 2. Change the password below.
# 3. NEVER commit the real secrets.toml — it is git-ignored on purpose.
#
# For the DEPLOYED app on Streamlit Community Cloud, do NOT use this file.
# Instead paste the same line into:
#   App  ->  ⋮ menu  ->  Settings  ->  Secrets
# ---------------------------------------------------------------------------

app_password = "change-this-to-a-strong-password"
